

const SectionTitle = ({title}) => {
    return (
        <div className="text-center my-5">
            <h2 className="lg:text-5xl md:text-3xl text-2xl text-blue-900 font-bold">{title}</h2>
        </div>
    );
};

export default SectionTitle;